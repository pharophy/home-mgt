## 1. Prompt And Variation Behavior

- [x] 1.1 Add failing server tests that capture interest-led prompt instructions and per-request variation for repeated completion images
- [x] 1.2 Update completion-image theme/variation selection so repeated requests do not collapse to the same visual recipe
- [x] 1.3 Strengthen the completion-image prompt so the selected child interests become explicit required visual motifs instead of weak optional inspiration

## 2. Verification

- [x] 2.1 Run the relevant server test suite
- [x] 2.2 Validate the OpenSpec change
