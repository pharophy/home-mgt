# Tasks

- [x] 1. Add a regression test showing a server workspace cwd resolves to the shared generated-assets directory in production.
- [x] 2. Update the managed generated-asset resolver to walk up from nested production paths and prefer the shared directory when present.
